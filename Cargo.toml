[package]
name = "rt890-flash"
version = "1.2.0"
edition = "2024"

[profile.dev]
overflow-checks = false

[profile.release]
lto = true
opt-level = "z"
strip = true

[dependencies]
serialport5 = "5.0.*"

[target.'cfg(unix)'.dependencies]
nix = "0.23.2"

[target.'cfg(windows)'.dependencies]
is_elevated = "0.1.*"
