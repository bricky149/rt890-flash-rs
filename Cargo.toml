[package]
name = "rt890-flash"
version = "1.2.97"
edition = "2024"

[profile.release]
codegen-units = 4
lto = "fat"
opt-level = "z"
panic = "abort"
strip = "symbols"

[dependencies]
serialport5 = { version = "5.0.*", default-features = false }
